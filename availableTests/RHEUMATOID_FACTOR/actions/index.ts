import { createUnconfigured } from "@availableTests/RHEUMATOID_FACTOR/actions/createUnconfigured";
import { configurePrice } from "@availableTests/RHEUMATOID_FACTOR/actions/configurePrice";
import { configureReferenceRanges } from "@availableTests/RHEUMATOID_FACTOR/actions/configureReferenceRanges";
import { updateResults } from "@availableTests/RHEUMATOID_FACTOR/actions/updateResults";

export const actions = {
  createUnconfigured,
  configurePrice,
  configureReferenceRanges,
  updateResults,
};
