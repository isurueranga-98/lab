import { createUnconfigured } from "@availableTests/FULL_BLOOD_COUNT/actions/createUnconfigured";
import { configurePrice } from "@availableTests/FULL_BLOOD_COUNT/actions/configurePrice";
import { configureReferenceRanges } from "@availableTests/FULL_BLOOD_COUNT/actions/configureReferenceRanges";
import { updateResults } from "@availableTests/FULL_BLOOD_COUNT/actions/updateResults";

export const actions = {
  createUnconfigured,
  configurePrice,
  configureReferenceRanges,
  updateResults,
};
