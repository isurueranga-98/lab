import { createUnconfigured } from "@availableTests/BILIRUBIN_TOTAL_AND_DIRECT/actions/createUnconfigured";
import { configurePrice } from "@availableTests/BILIRUBIN_TOTAL_AND_DIRECT/actions/configurePrice";
import { configureReferenceRanges } from "@availableTests/BILIRUBIN_TOTAL_AND_DIRECT/actions/configureReferenceRanges";
import { updateResults } from "@availableTests/BILIRUBIN_TOTAL_AND_DIRECT/actions/updateResults";

export const actions = {
  createUnconfigured,
  configurePrice,
  configureReferenceRanges,
  updateResults,
};
