import { createUnconfigured } from "@availableTests/THYROID_PROFILE/actions/createUnconfigured";
import { configurePrice } from "@availableTests/THYROID_PROFILE/actions/configurePrice";
import { configureReferenceRanges } from "@availableTests/THYROID_PROFILE/actions/configureReferenceRanges";
import { updateResults } from "@availableTests/THYROID_PROFILE/actions/updateResults";

export const actions = {
  createUnconfigured,
  configurePrice,
  configureReferenceRanges,
  updateResults,
};
