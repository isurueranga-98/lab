import { createUnconfigured } from "@availableTests/HBA1C_GLYCOSYLATED_HAEMOGLOBIN/actions/createUnconfigured";
import { configurePrice } from "@availableTests/HBA1C_GLYCOSYLATED_HAEMOGLOBIN/actions/configurePrice";
import { configureReferenceRanges } from "@availableTests/HBA1C_GLYCOSYLATED_HAEMOGLOBIN/actions/configureReferenceRanges";
import { updateResults } from "@availableTests/HBA1C_GLYCOSYLATED_HAEMOGLOBIN/actions/updateResults";

export const actions = {
  createUnconfigured,
  configurePrice,
  configureReferenceRanges,
  updateResults,
};
