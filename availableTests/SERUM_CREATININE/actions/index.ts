import { createUnconfigured } from "@availableTests/SERUM_CREATININE/actions/createUnconfigured";
import { configurePrice } from "@availableTests/SERUM_CREATININE/actions/configurePrice";
import { configureReferenceRanges } from "@availableTests/SERUM_CREATININE/actions/configureReferenceRanges";
import { updateResults } from "@availableTests/SERUM_CREATININE/actions/updateResults";

export const actions = {
  createUnconfigured,
  configurePrice,
  configureReferenceRanges,
  updateResults,
};
