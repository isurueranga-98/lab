import { createUnconfigured } from "@availableTests/ALBUMIN_SERUM/actions/createUnconfigured";
import { configurePrice } from "@availableTests/ALBUMIN_SERUM/actions/configurePrice";
import { configureReferenceRanges } from "@availableTests/ALBUMIN_SERUM/actions/configureReferenceRanges";
import { updateResults } from "@availableTests/ALBUMIN_SERUM/actions/updateResults";

export const actions = {
  createUnconfigured,
  configurePrice,
  configureReferenceRanges,
  updateResults,
};
