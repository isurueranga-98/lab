import { createUnconfigured } from "@availableTests/PROFILE_LIVER/actions/createUnconfigured";
import { configurePrice } from "@availableTests/PROFILE_LIVER/actions/configurePrice";
import { configureReferenceRanges } from "@availableTests/PROFILE_LIVER/actions/configureReferenceRanges";
import { updateResults } from "@availableTests/PROFILE_LIVER/actions/updateResults";

export const actions = {
  createUnconfigured,
  configurePrice,
  configureReferenceRanges,
  updateResults,
};
