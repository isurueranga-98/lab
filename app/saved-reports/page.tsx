import { PageContainer } from "@/components/layout/page-container.layout";
import React from "react";

const SavedReportsPage = (): React.JSX.Element => {
  return (
    <PageContainer title="Saved Reports">
      <div>
        <p>Saved reports will be displayed here.</p>
      </div>
    </PageContainer>
  );
};

export default SavedReportsPage;
